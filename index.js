const ignacioSvg = document.querySelector('#ignacio');
const cuadrosSvg = document.querySelector('#cuadros');
const ignacioSvgPositionTop = ignacioSvg.getBoundingClientRect().top;
const xUnit = ignacioSvg.height.baseVal.value / 2;

// cuadrosSvg.style.top = (ignacioSvgPositionTop + xUnit * 3) + 'px';

